# Recursive Countdown

This project counts down from a given number to 0 using a recursive function instead of a loop.

## What I Learned
- How to implement recursion in C
- How to define a base case to stop recursion
- How recursion replaces a simple loop
